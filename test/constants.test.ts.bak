import { INIT_CODE_HASH } from '../src/constants'

import { bytecode } from '@chimpyswap-libs/chimpy-swap-core/build/IChimpyPair.json'
import { keccak256 } from '@ethersproject/solidity'

// this _could_ go in constants, except that it would cost every consumer of the sdk the CPU to compute the hash
// and load the JSON.
const COMPUTED_INIT_CODE_HASH = keccak256(['bytes'], [`0x${bytecode}`])

describe('constants', () => {
  describe('INIT_CODE_HASH', () => {
    // it('matches computed bytecode hash', () => {
    //   expect(COMPUTED_INIT_CODE_HASH).toEqual(INIT_CODE_HASH)
    // })
    //TODO: Replace with real ChimpyPair.json
    it('matches computed bytecode hash', () => {
      expect(INIT_CODE_HASH).toEqual('0xcfcf24cfc50536b04eb8f7b60b50d72933c737e2148f585c86651345ed107e2f')
    })
  })
})
